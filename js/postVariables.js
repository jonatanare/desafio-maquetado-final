function generateFuntions( ){
    const postStructureId = ( ' ' ); 
    const headerImg = document.querySelector( ' ' );
    const ownerInfo = document.querySelector( ' ' );   
    const ownerImg = document.querySelector( ' ' );
    const ownerName = document.querySelector( ' ' );
    const postDescription = document.querySelector( ' ' );
    const postTitle = document.querySelector( ' ' );
    const postHashtags = document.querySelector( ' ' );
    const postContent = document.querySelector( ' ' );
    const postComments = document.querySelector( ' ' );
    const postCommentsTitle = document. querySelector( ' ' );  
    const buttonSubscribe = document.querySelector( ' ' );
    const userLogedComment = document.querySelector( ' ' );
    const userLogedImg = document.querySelector( ' ' );
    const userCommentInput = document.querySelector( ' ' );
    const allComments = document.querySelector( ' ' );
}

